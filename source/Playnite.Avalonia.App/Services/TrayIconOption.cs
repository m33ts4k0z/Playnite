namespace Playnite.Avalonia.App.Services;

// The system-tray icon variant. Mirrors the WPF TrayIconType (Default / Bright /
// Dark) so the tray icon can be made legible against light or dark taskbars.
public enum TrayIconOption
{
    Default,
    Bright,
    Dark
}
